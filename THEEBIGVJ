print("KINGVJHUB Executed")

local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local MainKeys = ReplicatedStorage
    :WaitForChild("events")
    :WaitForChild("MainKeys")


local Window = Rayfield:CreateWindow({
    Name = "KINGVJHUB",
    LoadingTitle = "KINGVJHUB",
    LoadingSubtitle = "By VJ",

    ConfigurationSaving = {
        Enabled = false
    },

    KeySystem = true,
    KeySettings = {
        Title = "KINGVJ KEY",
        Subtitle = "Enter The Key",
        Key = {"bigvj"} -- ✅ FIXED KEY
    }
})


Rayfield:Notify({
    Title = "KINGVJHUB",
    Content = "Loaded",
    Duration = 4
})


local MainTab = Window:CreateTab("Main", 4483362458)


task.wait(1)

if Window.KeySystemTab then
    Window.KeySystemTab:Destroy()
end


Rayfield:Notify({
    Title = "Access Granted",
    Content = "ENJOY",
    Duration = 4
})


-- AUTO DRAG

local dragging = false

MainTab:CreateToggle({
    Name = "Auto Drag 🚶‍♂️", -- ✅ emoji added
    CurrentValue = false,
    Callback = function(v)

        dragging = v

        if dragging then
            task.spawn(function()
                while dragging do
                    MainKeys:FireServer("Drag")
                    task.wait(0.2)
                end
            end)
        end

    end
})


-- AUTO PUSH

local pushing = false

MainTab:CreateToggle({
    Name = "Auto Push 🫸", -- ✅ emoji added
    CurrentValue = false,
    Callback = function(v)

        pushing = v

        if pushing then
            task.spawn(function()
                while pushing do
                    MainKeys:FireServer("Push")
                    task.wait(0.2)
                end
            end)
        end

    end
})


-- AUTO STOMP

local stomping = false

MainTab:CreateToggle({
    Name = "Auto Stomp 🦶", -- ✅ emoji added
    CurrentValue = false,
    Callback = function(v)

        stomping = v

        if stomping then
            task.spawn(function()
                while stomping do
                    MainKeys:FireServer("Stomp")
                    task.wait(0.2)
                end
            end)
        end

    end
})